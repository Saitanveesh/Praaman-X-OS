const nav = [{ id: 'dashboard', label: 'Dashboard' }, { id: 'drones', label: 'Drone Registry' }, { id: 'commands', label: 'Commands' }, { id: 'audit', label: 'Audit Log' }, { id: 'profiles', label: 'Profiles' }, { id: 'bridge', label: 'MISSION PLANNER BRIDGE' }, { id: 'sitl-readiness', label: 'SITL READINESS' }, { id: 'map-mission', label: 'MAP / MISSION' }];

export default function Layout({ page, setPage, children }: { page: string; setPage: (page: string) => void; children: React.ReactNode }) {
  return <div className="min-h-screen bg-[#050608]"><header className="border-b border-zinc-800 bg-black"><div className="mx-auto flex max-w-7xl flex-col gap-4 px-6 py-5 md:flex-row md:items-end md:justify-between"><div><p className="text-xs uppercase tracking-[0.32em] text-zinc-500">Shauryan Aerospace</p><h1 className="mt-1 text-2xl font-semibold text-zinc-100">Pramaan-X Intelligent C2 OS</h1></div><div className="text-xs uppercase tracking-widest text-zinc-500">Stage 1 Prototype / No Hardware Control</div></div></header><nav className="border-b border-zinc-800 bg-zinc-950"><div className="mx-auto flex max-w-7xl flex-wrap gap-2 px-6 py-3">{nav.map((item) => <button key={item.id} onClick={() => setPage(item.id)} className={`px-3 py-2 text-sm uppercase tracking-wide ${page === item.id ? 'border-b border-blue-400 text-white' : 'text-zinc-400 hover:text-zinc-100'}`}>{item.label}</button>)}</div></nav><main className="mx-auto max-w-7xl px-6 py-6">{children}</main></div>;
}
